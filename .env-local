# =============================================================================
# Facilitator Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# CDP Signer (Recommended for EVM)
# -----------------------------------------------------------------------------
# Get credentials from https://portal.cdp.coinbase.com/projects/api-keys

CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=
CDP_ACCOUNT_NAME=

# -----------------------------------------------------------------------------
# Private Key Signer (Fallback for EVM, Required for SVM)
# -----------------------------------------------------------------------------
# EVM: Only needed if CDP credentials are not provided
# SVM: Required (CDP doesn't support Solana yet)

# EVM_PRIVATE_KEY=
SVM_PRIVATE_KEY=

# -----------------------------------------------------------------------------
# EVM Networks
# -----------------------------------------------------------------------------
# Comma-separated list of EVM networks to enable
# Defaults to "base,base-sepolia" if not set
#
# Supported EVM networks:
#   abstract, abstract-testnet, arbitrum, arbitrum-sepolia,
#   avalanche, avalanche-fuji, base, base-sepolia,
#   ethereum, sepolia, optimism, optimism-sepolia,
#   polygon, polygon-amoy

EVM_NETWORKS=base,base-sepolia

# -----------------------------------------------------------------------------
# SVM (Solana) Networks
# -----------------------------------------------------------------------------
# Comma-separated list of Solana networks to enable
# Defaults to "solana-devnet" if not set (for safety)
#
# Supported SVM networks:
#   solana-mainnet, solana-devnet, solana-testnet

SVM_NETWORKS=solana-devnet

# -----------------------------------------------------------------------------
# Starknet Networks (Opt-in)
# -----------------------------------------------------------------------------
# Comma-separated list of Starknet networks to enable.
# Defaults to empty (disabled) unless set.
#
# Supported Starknet networks:
#   starknet-mainnet, starknet-sepolia

STARKNET_NETWORKS=starknet-mainnet,starknet-sepolia

# Sponsor address for /supported signers (required when Starknet is enabled)
STARKNET_SPONSOR_ADDRESS=

# Optional paymaster API key (AVNU sponsored mode)
STARKNET_PAYMASTER_API_KEY=

# -----------------------------------------------------------------------------
# EVM RPC Configuration (Simple)
# -----------------------------------------------------------------------------
# Just set your API key - URLs are auto-generated for each network!
#
# Option 1: Alchemy (recommended)
# Get your key from https://www.alchemy.com/

ALCHEMY_API_KEY=

# Option 2: Infura
# Get your key from https://www.infura.io/

# INFURA_API_KEY=

# -----------------------------------------------------------------------------
# SVM RPC Configuration (Simple)
# -----------------------------------------------------------------------------
# Just set your API key - URLs are auto-generated for each network!
#
# Helius (recommended for Solana)
# Get your key from https://www.helius.dev/

HELIUS_API_KEY=

# -----------------------------------------------------------------------------
# RPC Configuration (Advanced)
# -----------------------------------------------------------------------------
# Override specific network RPCs if needed
# Format: EVM_RPC_URL_<NETWORK_NAME>, SVM_RPC_URL_<NETWORK_NAME>,
# or STARKNET_RPC_URL_<NETWORK_NAME>
# (uppercase, hyphens become underscores)
#
# EVM Examples:
# EVM_RPC_URL_BASE=https://custom-base-rpc.example.com
# EVM_RPC_URL_BASE_SEPOLIA=https://custom-base-sepolia-rpc.example.com
#
# SVM Examples:
# SVM_RPC_URL_SOLANA_MAINNET=https://custom-solana-rpc.example.com
# SVM_RPC_URL_SOLANA_DEVNET=https://custom-devnet-rpc.example.com

# Starknet Examples:
# STARKNET_RPC_URL_STARKNET_MAINNET=https://starknet-mainnet.g.alchemy.com/v2/<key>
# STARKNET_RPC_URL_STARKNET_SEPOLIA=https://starknet-sepolia.g.alchemy.com/v2/<key>

# Paymaster endpoint overrides (optional)
# STARKNET_PAYMASTER_ENDPOINT_STARKNET_MAINNET=https://starknet.paymaster.avnu.fi
# STARKNET_PAYMASTER_ENDPOINT_STARKNET_SEPOLIA=http://localhost:12777

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

PORT=8090

# Override the facilitator URL used by examples (optional)
# FACILITATOR_URL=http://localhost:8090

# -----------------------------------------------------------------------------
# OpenTelemetry (Optional)
# -----------------------------------------------------------------------------

# OTEL_SERVICE_NAME=x402-facilitator
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
